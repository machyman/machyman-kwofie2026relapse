# Jekyll site dependencies. Pure-Ruby gems only: nothing here needs a native
# compiler or a JavaScript runtime, so `bundle install` cannot fail on a build
# machine. Math is rendered client-side by MathJax (see _includes/head_custom.html).
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.10"
gem "jekyll-seo-tag"
